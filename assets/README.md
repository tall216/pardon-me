# Placeholder assets folder for Pardon Me.
#
# Bundled for you:
#   * ringtone.wav  — synthesized telephone dual-tone ringtone (already present;
#                      referenced by src/fakeCall.ts and the call channel in
#                      src/scheduler.ts). For a real alarm-priority silent-bypass
#                      ring, also register this sound on the ALARM channel in a dev
#                      client — see src/fakeCall.ts and src/scheduler.ts.
#
# You may want to add:
#   * splash.png    — app splash/launch image (referenced by app.config.js).
#                     Not required for `tsc --noEmit`; only needed at the
#                     `expo start` / EAS build step.
